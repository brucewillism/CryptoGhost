# Datasets

Place sample or research datasets here.

**Raw/large datasets are gitignored.** Only small sample files or documentation should be committed.

Recommended structure:

```
datasets/
├── README.md          # This file
├── samples/           # Small demo datasets (safe to commit)
└── raw/               # Raw data (gitignored)
```
